{
  "name": "supermercado-pos",
  "version": "1.0.0",
  "description": "Sistema POS para supermercado",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/src/index.js"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
